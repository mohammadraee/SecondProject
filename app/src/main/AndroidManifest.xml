<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.google.secondproject">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SecondProject">
        <activity android:name=".RecyclerView.RecyclerViewActivity"></activity>
        <activity android:name=".Fragment.PreferenceFrag.FragPrefActivity" />
        <activity android:name=".Fragment.ActToActTranData.StuDetailActivity" />
        <activity android:name=".Fragment.FragmentActivity" />
        <activity android:name=".DataBase.DataBaseActivity" />
        <activity android:name=".IntroSlider.SlideShowV2" />
        <activity android:name=".IntroSlider.SlideShowV1" />
        <activity android:name=".IntroSlider.IntroSliderActivity" />
        <activity android:name=".SettingScreen.SettingScreenActivity" />
        <activity android:name=".Preference.TestSharingPrefActivity" />
        <activity android:name=".Preference.PrefActivity" />
        <activity android:name=".JsonXml.JsonXmlActivity" />
        <activity android:name=".File.FileActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>